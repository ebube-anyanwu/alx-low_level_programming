#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer.
 * @n: the character to be checked
 * Return: n
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-(n));
	}
}
